# 战斗体验雷达 v1｜实施前检查

- 日期：2026-07-28
- 当前分支：`project-main`
- 正式战斗与UI修改：无

## 检查结果

- 当前模拟入口：`scripts/simulate-random-dungeon.mjs`
- 当前固定 Seed 基准：`2026072701`
- 派生方式：`seedBase + (index + 1) * 7919`，按无符号32位整数处理
- 当前1000场是否可复现：是
- 当前可直接读取：逐局Seed、队伍、胜负、回合、最终妖力、存活数、换宠数、技能使用数、关卡与Boss ID
- 当前缺少：实际伤害、有效治疗、溢出治疗、护盾授予、妖力尝试与溢出、首次事件回合、Boss技能链、强化窗口
- 是否需要新增telemetry hook：是，仅增加默认关闭的测试专用只读collector
- 是否影响正式Demo：否，正式H5不传collector

## 文件范围

预计修改：

- `src/battle.ts`
- `scripts/simulate-random-dungeon.mjs`
- `package.json`
- `.github/workflows/battle-ci.yml`

预计新增：

- `src/battleTelemetry.ts`
- `scripts/experience-metrics.mjs`
- `scripts/render-experience-summary.mjs`
- `config/experience-thresholds.json`
- `tests/experience-metrics.test.mjs`
- `validation-baselines/README.md`

## 已确认统计口径

1. 保留当前固定Seed序列，不切换任务书示例Seed。
2. Boss核心机制按完整技能链体验计算。
3. 强化窗口按精灵每次正常行动机会计数；目标依赖技能存在至少一个满足条件的合法目标时计一次。
4. 第一轮不自动建立体验基线。
5. 只有殿下明确要求测试样本时，才运行自动测试、批量模拟并提交规定格式测试记录。

